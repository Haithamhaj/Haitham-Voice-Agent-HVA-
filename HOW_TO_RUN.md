# 🚀 دليل تشغيل Haitham Voice Agent

## الطريقة الأسهل ⭐

### على سطح المكتب:
ابحث عن ملف **`Start HVA.command`** على سطح المكتب وانقر عليه نقرتين

سيقوم الملف بـ:
- ✅ التحقق من البيئة الافتراضية
- ✅ التحقق من ملف `.env`
- ✅ تشغيل النظام تلقائياً
- ✅ عرض الأوامر المتاحة

---

## طرق التشغيل الأخرى

### 1️⃣ من Terminal (الطريقة الكاملة)

```bash
cd "/Users/haitham/development/Haitham Voice Agent (HVA)"
source .venv/bin/activate
python -m haitham_voice_agent.main
```

### 2️⃣ الملف القديم (Toggle)

```bash
./HVA_Toggle.command
```
- النقرة الأولى: تشغيل HVA
- النقرة الثانية: إيقاف HVA

### 3️⃣ وضع الاختبار (بدون صوت)

```bash
cd "/Users/haitham/development/Haitham Voice Agent (HVA)"
source .venv/bin/activate
python -m haitham_voice_agent.main --test "احفظ ملاحظة: اختبار النظام"
```

---

## المتطلبات الأساسية

### 1. ملف `.env` مع المفاتيح:

```bash
OPENAI_API_KEY=sk-your-key-here
GEMINI_API_KEY=your-key-here
```

### 2. البيئة الافتراضية:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 3. أذونات الميكروفون:
- System Preferences → Security & Privacy → Microphone
- تأكد من السماح لـ Terminal

---

## الأوامر الصوتية المتاحة

### 🧠 نظام الذاكرة
```
احفظ ملاحظة: اجتماع المشروع غداً الساعة 3
سجل فكرة: استخدام الذكاء الاصطناعي في التعليم
ايش آخر الملاحظات؟
ابحث في الملاحظات عن اجتماعات
```

### 📧 Gmail
```
اقرأ آخر إيميل
لخص آخر 5 إيميلات
اكتب مسودة إيميل لأحمد
ابحث في الإيميلات عن فواتير
```

### 📁 إدارة الملفات
```
اعرض الملفات في مجلد Downloads
ابحث عن ملفات PDF في المستندات
افتح مجلد المشاريع
أنشئ مجلد جديد اسمه AI-Project
```

### 📄 معالجة المستندات
```
لخص هذا الملف PDF
ترجم هذا المستند للإنجليزية
قارن بين هذين الملفين
```

### ✅ إدارة المهام
```
أضف مهمة: مراجعة الكود
اعرض مهامي
أكمل مهمة رقم 3
```

### 💻 أدوات النظام
```
افتح تطبيق Safari
افتح Chrome
اعرض معلومات النظام
```

### 🌐 المتصفح
```
افتح موقع google.com
ابحث في Google عن Python tutorials
```

---

## كيفية العمل

```
1. 🎤 تحدث بالعربية أو الإنجليزية
       ↓
2. 🤖 النظام يحول الكلام لنص (STT)
       ↓
3. 🧠 الذكاء الاصطناعي يفهم الأمر
       ↓
4. 📋 يعرض خطة التنفيذ للتأكيد
       ↓
5. ✅ تقول "نعم" للتأكيد
       ↓
6. ⚡ ينفذ الأمر
       ↓
7. 🔊 يرد عليك صوتياً بالنتيجة
```

---

## إيقاف النظام

### الطريقة 1: من Terminal
اضغط `Ctrl + C`

### الطريقة 2: من سطح المكتب
انقر نقرتين على `Start HVA.command` مرة أخرى (سيوقف النظام إذا كان يعمل)

### الطريقة 3: من Activity Monitor
ابحث عن `python` واقتل العملية

---

## استكشاف الأخطاء

### ❌ الميكروفون لا يعمل

```bash
# اختبر الميكروفون
python3 -c "import sounddevice as sd; print(sd.query_devices())"
```

**الحل:**
- تحقق من أذونات الميكروفون في System Preferences
- أعد تشغيل Terminal

### ❌ خطأ في API Keys

```bash
# تحقق من ملف .env
cat .env
```

**الحل:**
- تأكد من وجود المفاتيح
- تأكد من عدم وجود مسافات زائدة

### ❌ TTS لا يعمل

```bash
# اختبر صوت macOS
say -v Majed "مرحباً"
```

**الحل:**
- تأكد من تثبيت صوت Majed في System Preferences
- جرب صوت آخر

### ❌ البيئة الافتراضية غير موجودة

```bash
cd "/Users/haitham/development/Haitham Voice Agent (HVA)"
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

---

## نصائح للاستخدام الأمثل

### 🎯 للحصول على أفضل دقة:
- تحدث بوضوح وببطء
- استخدم جمل قصيرة ومباشرة
- تجنب الضوضاء في الخلفية

### 🚀 للأوامر السريعة:
- استخدم الأوامر المباشرة
- مثال: "احفظ ملاحظة: [المحتوى]"

### 📝 للجلسات الطويلة:
- قل "ابدأ جلسة"
- تحدث بحرية
- قل "أوقف الجلسة" عند الانتهاء

---

## الملفات المهمة

- **Start HVA.command** - تشغيل النظام (موصى به)
- **HVA_Toggle.command** - تشغيل/إيقاف النظام
- **RUNNING.md** - دليل التشغيل التفصيلي
- **README.md** - دليل المشروع الكامل

---

## الدعم

للمشاكل التقنية:
1. راجع قسم استكشاف الأخطاء أعلاه
2. تحقق من السجلات في `~/.hva_logs/hva.log`
3. راجع التوثيق في `README.md`

---

**جاهز للبدء؟ انقر نقرتين على `Start HVA.command` على سطح المكتب! 🚀**
